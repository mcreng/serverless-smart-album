# Web Portal

## Development

```sh
npm i
npm start
```

## Production in Docker(WIP)

```sh
# building the image
docker build -t recognition-web .

# run the image
docker run -it -p 8080:80 recognition-web
```

